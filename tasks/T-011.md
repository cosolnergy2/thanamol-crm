# T-011: i18n system — LanguageProvider full translation dictionary

**Status:** In Progress
**Assignee:** dev
**Created:** 2026-03-15
**Updated:** 2026-03-15

### Objective

Extend the existing `LanguageProvider` (created in T-007) with the complete translation dictionary from base44. T-007 created the provider with a minimal set of keys; this task ports ALL translation keys used across the 135 application pages.

**Existing file to update:** `apps/web/src/providers/LanguageProvider.tsx`

**Source to port from:** `/Users/jettapat/personal/7tapat/projects/propertyflow-crm/src/components/translations.jsx`

**What to do:**
- Read all translation keys from `translations.jsx`
- Merge them into the `en` and `th` dictionaries in `LanguageProvider.tsx`
- Do not remove any keys already present from T-007 — only add missing ones
- The `t()` return type should remain strongly typed (TypeScript `satisfies` or `as const` pattern)
- Language preference continues to persist in `localStorage` under the existing key

**This is a large file.** The base44 translations cover every label, button, heading, placeholder, error message, and status string used across all modules: properties, customers, leads, finance, contracts, documents, reports, settings, etc.

**Acceptance criteria:**
1. All keys from base44 `translations.jsx` are present in both `en` and `th` dictionaries
2. `t('someKey')` returns the correct string in the active language
3. No previously existing keys are removed or renamed
4. Language toggle still persists to `localStorage`
5. Unit tests verify a representative sample of keys in both languages (at minimum 20 key/value assertions)

### Detail

**Implementation approach:**
- Extract all translation key/value pairs from base44 `translations.jsx` (both `en` and `th` objects)
- Move translation dictionaries to `apps/web/src/lib/translations.ts` to keep LanguageProvider focused on context logic
- Merge base44 keys with existing T-007 keys — no removals, only additions; existing keys from T-007 take precedence where both exist
- Import the `en` and `th` dictionaries back into `LanguageProvider.tsx`; the `Translations` type continues to be derived from `typeof en`
- Missing-key fallback: `t()` via the typed object already returns `undefined` for missing keys — keep this as-is (TypeScript enforces key existence at compile time)
- Extend unit tests to assert at minimum 20 representative key/value pairs across both languages

**Files to create or modify:**
- `apps/web/src/lib/translations.ts` — NEW: full merged `en` and `th` dictionaries
- `apps/web/src/providers/LanguageProvider.tsx` — MODIFY: import from `../lib/translations` instead of defining inline
- `apps/web/src/providers/LanguageProvider.test.tsx` — MODIFY: add 20+ key assertions

**No API endpoints or database changes.**

**Documentation referenced:**
- https://www.typescriptlang.org/docs/handbook/2/typeof-types.html

### Definition of Done

[Tester fills this — specific test scenarios, expected behaviors, E2E test file paths]

### Test Results

[Tester fills this — pass/fail, screenshot paths, error logs if failed]
