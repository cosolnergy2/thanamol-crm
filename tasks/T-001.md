# T-001: Docker Compose + Prisma schema (42 models)

**Status:** Ready to Test
**Assignee:** dev
**Created:** 2026-03-15
**Updated:** 2026-03-15

### Objective

Set up local PostgreSQL via Docker Compose and create the complete Prisma schema with all 42 models so the API has a fully-typed database layer from day one.

**Docker Compose requirements:**
- PostgreSQL 18 image (`postgres:18`)
- Database: `thanamol_crm`, User: `thanamol`, Password: `thanamol_secret`
- Port mapping: `5432:5432`
- Named volume for data persistence
- Health check (`pg_isready`)

**Prisma schema — 42 models grouped by domain:**

Auth domain:
- `User` — id, email, password_hash, first_name, last_name, avatar_url, is_active, created_at, updated_at
- `Role` — id, name, description, permissions (Json), created_at
- `UserRole` — user_id (FK User), role_id (FK Role), composite PK
- `UserAuditLog` — id, user_id (FK User), action, details, ip_address, created_at

CRM domain:
- `Customer` — id, name, email, phone, address, tax_id, type (enum), status, notes, created_at, updated_at
- `Contact` — id, customer_id (FK Customer), first_name, last_name, email, phone, position, is_primary, created_at
- `Company` — id, name, tax_id, address, phone, email, website, industry, status, notes, created_at, updated_at

Sales domain:
- `Lead` — id, title, customer_id (FK), contact_id (FK), source, status, value, probability, expected_close_date, notes, assigned_to (FK User), created_at, updated_at
- `Deal` — id, title, customer_id (FK), lead_id (FK), stage, value, probability, expected_close_date, actual_close_date, notes, assigned_to (FK User), created_at, updated_at

Property domain:
- `Project` — id, name, code, description, address, type, status, total_units, settings (Json), created_at, updated_at
- `ProjectTemplate` — id, name, description, settings (Json), created_at
- `Unit` — id, project_id (FK Project), unit_number, floor, building, type, area_sqm, price, status, features (Json), created_at, updated_at

Sales Documents domain:
- `Quotation` — id, quotation_number, customer_id (FK), project_id (FK), unit_id (FK), items (Json), total_amount, discount, tax, grand_total, status, valid_until, notes, approved_by (FK User), created_by (FK User), created_at, updated_at
- `CommercialQuotation` — id, quotation_number, customer_id (FK), project_id (FK), items (Json), terms, conditions, total_amount, status, created_by (FK User), created_at, updated_at
- `CommercialProposal` — id, proposal_number, customer_id (FK), project_id (FK), content (Json), status, created_by (FK User), created_at, updated_at

Contracts domain:
- `Contract` — id, contract_number, customer_id (FK), project_id (FK), unit_id (FK), quotation_id (FK), type, start_date, end_date, value, monthly_rent, deposit_amount, terms, status, approved_by (FK User), created_by (FK User), created_at, updated_at
- `LeaseAgreement` — id, contract_id (FK Contract), lease_terms (Json), special_conditions, status, created_at, updated_at
- `PreHandoverInspection` — id, contract_id (FK), inspection_date, inspector, items (Json), overall_status, notes, photos (Json), created_at, updated_at

Handover domain:
- `Handover` — id, contract_id (FK), handover_date, handover_type, checklist (Json), notes, status, received_by, handed_by (FK User), created_at, updated_at
- `HandoverPhotos` — id, handover_id (FK Handover), photos (Json), description, category, created_at

Finance domain:
- `Invoice` — id, invoice_number, contract_id (FK), customer_id (FK), items (Json), subtotal, tax, total, due_date, status, notes, created_by (FK User), created_at, updated_at
- `Payment` — id, invoice_id (FK Invoice), amount, payment_date, payment_method, reference_number, notes, received_by (FK User), created_at
- `Deposit` — id, contract_id (FK), customer_id (FK), amount, deposit_date, status, refund_date, refund_amount, notes, created_at, updated_at
- `MeterRecord` — id, unit_id (FK Unit), meter_type, previous_reading, current_reading, reading_date, usage, amount, billing_period, created_at

Tasks domain:
- `Task` — id, title, description, project_id (FK), assigned_to (FK User), created_by (FK User), priority, status, due_date, parent_task_id (FK self Task), estimated_hours, actual_hours, tags (Json), is_recurring, recurrence_pattern, created_at, updated_at
- `TaskComment` — id, task_id (FK Task), user_id (FK User), content, created_at
- `TaskStatus` — id, name, color, order, is_default, is_closed, created_at
- `AutomationRule` — id, name, trigger_event, conditions (Json), actions (Json), is_active, created_at
- `Ticket` — id, title, description, customer_id (FK), unit_id (FK), category, priority, status, assigned_to (FK User), resolved_at, created_at, updated_at

Documents domain:
- `Document` — id, title, file_url, file_type, file_size, category, entity_type, entity_id, uploaded_by (FK User), tags (Json), version, created_at, updated_at
- `FormTemplate` — id, name, description, fields (Json), category, is_active, created_at
- `PDFTemplateSettings` — id, name, template_type, header (Json), footer (Json), styles (Json), is_default, created_at, updated_at
- `ISODocument` — id, document_number, title, category, revision, status, content, effective_date, review_date, approved_by (FK User), created_at, updated_at

Meetings domain:
- `MeetingMinute` — id, title, meeting_date, location, attendees (Json), agenda (Json), minutes (Json), action_items (Json), pdf_url, status, created_by (FK User), created_at, updated_at
- `MeetingTemplate` — id, name, description, sections (Json), created_at
- `MeetingPDFTemplate` — id, name, layout (Json), styles (Json), header (Json), footer (Json), is_default, created_at

System domain:
- `Notification` — id, user_id (FK User), title, message, type, entity_type, entity_id, is_read, created_at
- `NotificationPreference` — id, user_id (FK User), notification_type, email_enabled, in_app_enabled, created_at
- `ActivityLog` — id, user_id (FK User), action, entity_type, entity_id, details (Json), ip_address, created_at
- `Comment` — id, entity_type, entity_id, user_id (FK User), content, created_at, updated_at
- `ClientComment` — id, entity_type, entity_id, client_user_id (FK ClientUser), content, created_at
- `ClientUpdateRequest` — id, entity_type, entity_id, client_user_id (FK ClientUser), requested_changes (Json), status, reviewed_by (FK User), created_at, updated_at
- `ClientUser` — id, customer_id (FK Customer), email, password_hash, first_name, last_name, is_active, created_at
- `WarehouseRequirement` — id, customer_id (FK), project_id (FK), requirements (Json), specifications (Json), status, created_by (FK User), created_at, updated_at
- `SaleJob04F01` — id, form_number, project_id (FK), customer_id (FK), unit_id (FK), form_data (Json), status, created_by (FK User), approved_by (FK User), created_at, updated_at

**Acceptance criteria:**
1. `docker compose up -d` starts PostgreSQL 18 and it is accessible on localhost:5432
2. `npx prisma migrate dev` runs without errors
3. All 42 models are present with proper relations and indexes
4. `apps/api/.env` has `DATABASE_URL` pointing to the Docker PostgreSQL instance

### Detail

**Implementation approach:**
- Create `docker-compose.yml` at project root with PostgreSQL 18, named volume, and health check
- Create `apps/api/prisma/schema.prisma` with all 42 models organized by domain
- Define all enums for status fields: CustomerType, UnitStatus, ContractStatus, InvoiceStatus, PaymentMethod, Priority, LeadStatus, DealStage, TaskStatus (enum), ContractType, HandoverType, DepositStatus, MeterType, TicketStatus, DocumentStatus
- Add @@index on all foreign key fields for query performance
- Verify `apps/api/.env` has correct DATABASE_URL (already present)

**Files created/modified:**
- `docker-compose.yml` (new) — PostgreSQL 18 service with health check
- `apps/api/prisma/schema.prisma` (new) — full 45-model schema (task listed 42 but specifies 45 distinct models)
- `apps/api/prisma.config.ts` (new) — Prisma 7 datasource config pointing to DATABASE_URL
- `apps/api/prisma/schema.test.ts` (new) — Vitest tests verifying all models, enums, indexes
- `tasks/T-001.md` (modified) — status updates

**Documentation referenced:**
- https://www.prisma.io/docs/orm/reference/prisma-schema-reference
- https://www.prisma.io/docs/orm/prisma-schema/data-model/relations
- https://www.prisma.io/docs/orm/reference/prisma-config-reference (Prisma 7 config format)

**Notes:**
- Prisma 7 requires `prisma.config.ts` at the app root — the `url` field is no longer valid in the `datasource` block of `schema.prisma`
- The generator is now `prisma-client` (not `prisma-client-js`) and outputs to `../generated/prisma`
- `apps/api/.env` already contained the correct DATABASE_URL
- The task says "42 models" but the spec body lists 45 distinct model names — all 45 are implemented

### Definition of Done

[Tester fills this — specific test scenarios, expected behaviors, E2E test file paths]

### Test Results

[Tester fills this — pass/fail, screenshot paths, error logs if failed]
