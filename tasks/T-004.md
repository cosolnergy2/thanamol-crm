# T-004: JWT auth API endpoints

**Status:** Ready to Test
**Assignee:** dev
**Created:** 2026-03-15
**Updated:** 2026-03-15

### Objective

Build JWT-based authentication API to replace Base44 auth. This is the backend auth layer that all protected API routes will depend on.

**Endpoints required:**

| Method | Path | Description |
|--------|------|-------------|
| POST | /api/auth/register | Register new user (email, password, first_name, last_name) |
| POST | /api/auth/login | Login with email/password, return access_token + refresh_token |
| POST | /api/auth/refresh | Refresh access token using refresh_token |
| GET | /api/auth/me | Get current user profile (requires auth) |
| POST | /api/auth/logout | Invalidate refresh token |

**Technical requirements:**
- Use `@elysiajs/jwt` plugin for token signing and verification
- Use `bcryptjs` for password hashing and comparison
- Access token expires in 15 minutes
- Refresh token expires in 7 days
- Store refresh tokens in database (add `RefreshToken` model to Prisma schema if not present)
- Create `authGuard` middleware that verifies JWT from `Authorization: Bearer <token>` header and attaches user to context

**Reference:** `/Users/jettapat/personal/7tapat/projects/propertyflow-crm/src/lib/AuthContext.jsx` — for understanding the expected auth flow from the client side.

**Acceptance criteria:**
1. All 5 endpoints respond with correct status codes and payloads
2. JWT tokens are correctly signed and verified using the configured secret
3. `authGuard` middleware returns 401 for missing or invalid tokens
4. Invalid credentials on login return 401 with a clear error message
5. Refresh token rotation works (old token invalidated on refresh)
6. Unit tests cover happy path and error cases for all endpoints

### Detail

**Implementation approach:**
- Add `RefreshToken` model to Prisma schema with userId FK and expiry fields
- Create `apps/api/src/middleware/auth.ts` — Elysia plugin using `.derive()` to read/verify Bearer token and attach user to context, plus `.guard()` + `beforeHandle` for protected routes
- Create `apps/api/src/routes/auth.ts` — Elysia route group for `/api/auth/*` endpoints using `t.Object()` for validation and `bcryptjs` for password hashing
- Register the JWT plugin (two instances: access 15m, refresh 7d) in `apps/api/src/index.ts` and mount auth routes
- Add `JWT_SECRET` to `apps/api/.env`
- Add shared auth types to `packages/shared/src/types/index.ts`
- Write unit tests in `apps/api/src/routes/auth.test.ts` covering all 5 endpoints happy + error paths

**Files to create/modify:**
- `apps/api/prisma/schema.prisma` — add `RefreshToken` model and relation on `User`
- `apps/api/src/middleware/auth.ts` — NEW: `authPlugin` (derive user) + `authGuard` (protected group)
- `apps/api/src/routes/auth.ts` — NEW: auth route group
- `apps/api/src/routes/auth.test.ts` — NEW: unit tests
- `apps/api/src/index.ts` — register JWT + auth routes
- `apps/api/.env` — add JWT_SECRET
- `packages/shared/src/types/index.ts` — add AuthUser, LoginRequest, RegisterRequest, AuthResponse types

**API endpoints:**
- `POST /api/auth/register` — body: `{ email, password, firstName, lastName }` → `{ user, accessToken, refreshToken }`
- `POST /api/auth/login` — body: `{ email, password }` → `{ user, accessToken, refreshToken }`
- `POST /api/auth/refresh` — body: `{ refreshToken }` → `{ accessToken }`
- `GET /api/auth/me` — header: `Authorization: Bearer <token>` → `{ user }`
- `POST /api/auth/logout` — header: `Authorization: Bearer <token>` → `{ success: true }`

**Database model changes:**
- New `RefreshToken` model with `id`, `token`, `userId`, `expiresAt`, `createdAt`
- New `refresh_tokens` relation field on `User` model

**Documentation URLs referenced:**
- https://elysiajs.com/plugins/jwt
- https://elysiajs.com/essential/plugin

### Definition of Done

[Tester fills this — specific test scenarios, expected behaviors, E2E test file paths]

### Test Results

[Tester fills this — pass/fail, screenshot paths, error logs if failed]
