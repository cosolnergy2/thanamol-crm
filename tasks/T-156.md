# T-156: Update shared types and constants for all new schema fields

**Status:** In Progress
**Assignee:** dev
**Created:** 2026-04-01
**Updated:** 2026-04-01

### Objective

After T-152, T-153, and T-154 add new Prisma schema fields, this task ensures the shared package reflects all those changes with proper TypeScript types and constant arrays for every new dropdown value identified in the bug report.

This is a consolidation task — it may overlap partially with T-152/T-153/T-154 but the goal here is a complete audit and alignment pass rather than migration-time additions.

Files:
- `packages/shared/src/types/index.ts`
- `packages/shared/src/constants/fms.ts`

Acceptance criteria: `pnpm build` passes across all three packages with zero type errors, and every dropdown referenced in the bug report has a corresponding constant array.

### Detail

**Implementation approach:**
- Checked out the T-152 branch diff to identify all new schema fields added to Asset, WorkOrder, PreventiveMaintenance, CalibrationRecord, InventoryItem, StockIssue, GoodsReceivedNote, PettyCashTransaction, PurchaseRequest, PurchaseOrder, Vendor, VendorContract, and VendorInvoice models
- Added all new fields to their corresponding TypeScript types in `packages/shared/src/types/index.ts` as optional fields matching Prisma nullability exactly
- Added all new fields to corresponding Create/Update request types
- Added all required dropdown constant arrays to `packages/shared/src/constants/fms.ts`

**Files changed:**
- `packages/shared/src/types/index.ts` — added new fields to 13 model types and their request types
- `packages/shared/src/constants/fms.ts` — added 17 new constant arrays for dropdown values

**Documentation referenced:**
- Prisma schema diff from feat/T-152-schema-migrations-phase1 branch

### Definition of Done

- `pnpm build` passes across all packages (api, web, shared) with no type errors
- All types in `packages/shared/src/types/index.ts` match the updated Prisma schema exactly
- Constants in `packages/shared/src/constants/fms.ts` cover all dropdown values from the bug report screenshots (scope types, criticality levels, lifecycle statuses, condition scores, trigger types, item types, QC statuses, PR purposes, PO types, vendor types, contract types, etc.)

### Test Results

_Tester fills this — pass/fail, screenshot paths, error logs if failed_
