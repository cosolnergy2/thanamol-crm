# T-152: Prisma schema migration — Asset, WorkOrder, PM, Calibration fields

**Status:** Approved
**Assignee:** dev
**Created:** 2026-04-01
**Updated:** 2026-04-01

### Objective

Add missing fields to four Prisma models that FMS forms require. These schema changes are the foundation for Phases 2 and 3.

Fields to add:
- **Asset:** `scope_type`, `brand`, `supplier_id`, `install_date`, `criticality`, `condition_score`, `lifecycle_status`
- **WorkOrder:** `budget_code`, `scheduled_start`, `scheduled_end`, `vendor_id`
- **PreventiveMaintenance:** `scope_type`, `trigger_type`, `estimated_duration`, `spare_parts` (Json), `auto_create_wo`, `auto_wo_days_before`
- **CalibrationRecord:** `calibration_number`, `frequency_days`, `calibration_type`, `calibration_standard`, `certificate_number`, `cost`, `results` (Json)

Acceptance criteria: migration runs cleanly, build passes, no existing tests break.

### Detail

_Dev fills this — implementation plan, technical approach, files changed, doc URLs referenced_

### Definition of Done

- Migration runs successfully (`npx prisma migrate dev`)
- `pnpm build` passes with no type errors
- Existing unit tests still pass (`pnpm test`)
- E2E: verify app starts and existing FMS pages load without errors

### Test Results

_Tester fills this — pass/fail, screenshot paths, error logs if failed_
