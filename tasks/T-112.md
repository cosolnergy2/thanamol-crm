# T-112: FMS Foundation — sidebar navigation + Zone management pages

**Status:** Ready to Test
**Assignee:** dev
**Created:** 2026-03-25
**Updated:** 2026-03-25

### Objective
Add FMS section to the sidebar navigation and create Zone management pages (list, create, edit). This completes Phase 0 of the Facility Management System by wiring the UI to the Zone API from T-111.

Acceptance criteria:
- "Facility Management" collapsible section visible in sidebar
- Zone list, create, and edit pages are functional
- `pnpm build` passes

### Detail
**Branch:** `feat/T-112-fms-sidebar-zones-ui`
**Depends on:** T-111

1. **Sidebar** (`apps/web/src/components/layout/Sidebar.tsx`):
   Add "Facility Management" collapsible section with sub-items:
   - Dashboard (placeholder for Phase 9)
   - Zones (link to /facility-management/zones)
   - Assets (placeholder, disabled)
   - Work Orders (placeholder, disabled)
   - Inventory (placeholder, disabled)

2. **Zone pages** (NEW files under `apps/web/src/routes/_authenticated/facility-management/`):
   - `zones/index.tsx` — Zone list page (filter by project, show hierarchy)
   - `zones/create.tsx` — Create zone form (project dropdown, name, code, floor, building, parent zone)
   - `zones/$zoneId/edit.tsx` — Edit zone

3. **Hooks** (`apps/web/src/hooks/useZones.ts` — NEW):
   - useZones(projectId), useZone(id), useCreateZone(), useUpdateZone(), useDeleteZone()

### Implementation Detail

- Merged `feat/T-111-zone-api` to get Zone API and shared types
- Added `ZoneWithCount` type to shared types (extends `Zone` with `_count: { children, units }`)
- Created `useZones.ts` hook following `useProjects.ts` pattern with `ZONE_QUERY_KEYS` and 5 CRUD hooks
- Extended `NavSubItem` type with `disabled?: boolean` for placeholder FMS menu items
- Added `Factory` icon + "Facility Management" collapsible section with 5 sub-items (Assets/Work Orders/Inventory disabled)
- Created 4 route files under `_authenticated/facility-management/` and updated `routeTree.gen.ts`

**Files created/modified:**
- `packages/shared/src/types/index.ts`
- `apps/web/src/hooks/useZones.ts`
- `apps/web/src/hooks/useZones.test.ts`
- `apps/web/src/components/layout/Sidebar.tsx`
- `apps/web/src/routes/_authenticated/facility-management/index.tsx`
- `apps/web/src/routes/_authenticated/facility-management/zones/index.tsx`
- `apps/web/src/routes/_authenticated/facility-management/zones/create.tsx`
- `apps/web/src/routes/_authenticated/facility-management/zones/$zoneId/edit.tsx`
- `apps/web/src/routeTree.gen.ts`

### Definition of Done

- [x] "Facility Management" collapsible section visible in sidebar
- [x] Zone list page with project filter and CRUD actions
- [x] Zone create form with project dropdown, code, name, floor, building, parent zone
- [x] Zone edit form pre-populated from API
- [x] `pnpm --filter web build` passes
- [x] `pnpm --filter web test` passes (416/416)

### Test Results
