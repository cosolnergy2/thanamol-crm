# T-153: Prisma schema migration — Inventory, Stock, GRN, PettyCash fields

**Status:** Approved
**Assignee:** dev
**Created:** 2026-04-01
**Updated:** 2026-04-01

### Objective

Add missing fields to four Prisma models that Inventory and Finance forms require. Depends on T-152 being applied first (same migration session is fine).

Fields to add:
- **InventoryItem:** `item_type`, `barcode`, `company_id`, `site_id`, `specifications` (Json), `vendor_id`, `lead_time_days`, `photos` (Json)
- **StockIssue:** `purpose`
- **GoodsReceivedNote:** `po_id` (FK to PurchaseOrder), `qc_status`
- **PettyCashTransaction:** `site_id`, `budget_code`, `responsible_person_id`

Also add new shared constants: `INVENTORY_ITEM_TYPES`, `STOCK_ISSUE_PURPOSES`, `QC_STATUSES`.

Acceptance criteria: migration runs cleanly, build passes, no existing tests break.

### Detail

_Dev fills this — implementation plan, technical approach, files changed, doc URLs referenced_

### Definition of Done

- Migration runs successfully (`npx prisma migrate dev`)
- `pnpm build` passes with no type errors
- Existing unit tests still pass (`pnpm test`)
- E2E: inventory, stock issue, GRN, and petty cash list pages load without errors

### Test Results

_Tester fills this — pass/fail, screenshot paths, error logs if failed_
