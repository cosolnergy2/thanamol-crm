# T-006: Port 49 Radix UI components

**Status:** Draft
**Assignee:** dev
**Created:** 2026-03-15
**Updated:** 2026-03-15

### Objective

Port all Radix UI primitive components from the base44 project to thanamol-crm. These components are the foundational UI building blocks that every page depends on. No feature pages can be built until this task is complete.

**Source:** `/Users/jettapat/personal/7tapat/projects/propertyflow-crm/src/components/ui/`
**Target:** `/Users/jettapat/personal/7tapat/projects/thanamol-crm/apps/web/src/components/ui/`

**Port all components from the source directory.** The full list includes but is not limited to:

accordion, alert, alert-dialog, aspect-ratio, avatar, badge, button, calendar, card, carousel, chart, checkbox, collapsible, command, date-picker, dialog, drawer, dropdown-menu, form, input, input-otp, label, menubar, navigation-menu, pagination, popover, progress, radio-group, resizable, scroll-area, select, separator, sheet, skeleton, slider, sonner, switch, table, tabs, textarea, toast, toaster, toggle, toggle-group, tooltip

**Adaptation rules:**
- Update `@/lib/utils` import to the correct path within `apps/web/src/`
- Ensure `cn()` utility exists at `apps/web/src/lib/utils.ts`
- Tailwind v4 may require class name adjustments — check and fix any that break
- Keep visual appearance identical to base44
- Keep all Radix UI prop APIs identical (do not rename props)
- Use the project's indigo/teal design tokens (no arbitrary colors)

**Acceptance criteria:**
1. Every component file from the base44 ui/ directory exists in target
2. `pnpm build` passes with no TypeScript errors in the ui/ directory
3. Components render correctly (confirmed by dev via Storybook or manual inspection)
4. No arbitrary color values — only Tailwind design tokens used

### Detail

[Dev fills this — implementation plan, technical approach, files changed, doc URLs referenced]

### Definition of Done

[Tester fills this — specific test scenarios, expected behaviors, E2E test file paths]

### Test Results

[Tester fills this — pass/fail, screenshot paths, error logs if failed]
