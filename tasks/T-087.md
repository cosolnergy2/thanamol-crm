# T-087: Quotation form — add Cost & Responsibility section

**Status:** In Progress
**Assignee:** dev
**Branch:** fix/T-087-quotation-missing-fields

## Objective

Add Cost & Responsibility section with 12 new Prisma fields and corresponding frontend form sections to the Quotation create and edit forms.

## Acceptance Criteria

- 12 new columns added to Quotation model in Prisma
- Shared constants file for RATE_TYPES, RESPONSIBILITY_OPTIONS, DEPOSIT_DECORATION_OPTIONS, DURATION_UNITS, RENT_CALC_UNITS
- QuotationItem type expanded with optional fields
- API create/update routes accept and persist the new fields
- Create form has Cost & Deposit, Utility Rates, Responsibility, and Conditions sections
- Edit form mirrors the same sections and populates from existing quotation data
- pnpm build passes with zero errors
- pnpm test passes

## Detail

### Implementation Approach

- Add new constants file `packages/shared/src/constants/quotation.ts`
- Update constants barrel `packages/shared/src/constants/index.ts`
- Add 12 columns to Prisma Quotation model and run migration
- Expand Quotation and QuotationItem types in shared types
- Update API create/update schemas and Prisma data mappings
- Add new form sections to both create.tsx and edit.tsx

### Files to Create or Modify

- `packages/shared/src/constants/quotation.ts` — NEW
- `packages/shared/src/constants/index.ts` — UPDATE (add re-export)
- `packages/shared/src/types/index.ts` — UPDATE Quotation, QuotationItem, CreateQuotationRequest
- `apps/api/prisma/schema.prisma` — ADD 12 columns to Quotation model
- `apps/api/src/routes/quotations.ts` — ADD fields to create/update schemas
- `apps/web/src/routes/_authenticated/quotations/create.tsx` — ADD new sections
- `apps/web/src/routes/_authenticated/quotations/$quotationId/edit.tsx` — ADD new sections

### Documentation URLs Referenced

- https://www.prisma.io/docs/orm/reference/prisma-schema-reference
- https://elysiajs.com/validation/elysia-type
- https://tanstack.com/router/latest/docs/framework/react/guide/file-based-routing

## Test Results

_To be filled by tester_
