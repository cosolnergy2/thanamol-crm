# T-073: Register All Missing API Routes

**Status:** Test Pass
**Assignee:** tester
**Created:** 2026-03-19
**Updated:** 2026-03-19

### Objective
Several route files exist in `apps/api/src/` but are never mounted in `apps/api/src/index.ts`, meaning the endpoints they define return 404 in production. The goal is to find all 17 unregistered route files and mount them in the main app entry point so every endpoint in the codebase is reachable.

Acceptance criteria:
- All route files under `apps/api/src/` are imported and registered in `apps/api/src/index.ts`.
- No existing registered routes are removed or reordered in a breaking way.
- `pnpm build` passes with no TypeScript errors.
- A smoke-test GET to each newly registered route base path returns something other than 404.

### Detail
- Audited all files under `apps/api/src/routes/` and compared against imports in `apps/api/src/index.ts`.
- Found 17 route exports not yet mounted: activity-logs, automation-rules, client-portal, comments, deposits, handover-photos, handovers, lease-agreements, meter-records, notifications (x2), pre-handover-inspections, sale-jobs, task-config, task-statuses, tasks, tickets, warehouse-requirements.
- Added missing imports and `.use()` calls to `apps/api/src/index.ts` preserving all existing registrations and order.

Files changed:
- `apps/api/src/index.ts` — added 17 imports and `.use()` calls
- `tasks/T-073.md` — status updated to In Progress

### Definition of Done
- GET /api/activity-logs returns 401 (not 404) — route is registered, auth guard is active
- GET /api/audit-logs returns 401 (not 404)
- GET /api/automation-rules returns 401 (not 404)
- GET /api/clients returns 401 (not 404) — client-portal routes
- GET /api/client-update-requests returns 401 (not 404)
- GET /api/comments returns 401 (not 404) — auth guard fires before param validation
- GET /api/deposits returns 401 (not 404)
- GET /api/handover-photos returns 401 (not 404)
- GET /api/handovers returns 401 (not 404)
- GET /api/lease-agreements returns 401 (not 404)
- GET /api/meter-records returns 401 (not 404)
- GET /api/notifications returns 401 (not 404)
- GET /api/notification-preferences returns 401 (not 404)
- GET /api/pre-handover-inspections returns 401 (not 404)
- GET /api/sale-jobs returns 401 (not 404)
- GET /api/task-statuses returns 401 (not 404)
- GET /api/tasks returns 401 (not 404)
- GET /api/tickets returns 401 (not 404)
- GET /api/warehouse-requirements returns 401 (not 404)

### Test Results

**Result: PASS — 20/20 tests passed**

**Test file:** `apps/web/e2e/T-073-route-registration.spec.ts`

**Command:**
```
pnpm --filter web exec playwright test apps/web/e2e/T-073-route-registration.spec.ts --reporter=list
```

**Screenshot:** `apps/web/e2e/screenshots/T-073/api-health-ok.png`

**DoD coverage:**

| DoD Item | Test | Result |
|---|---|---|
| GET /api/activity-logs → 401 | "GET /api/activity-logs returns 401, not 404" | PASS |
| GET /api/audit-logs → 401 | "GET /api/audit-logs returns 401, not 404" | PASS |
| GET /api/automation-rules → 401 | "GET /api/automation-rules returns 401, not 404" | PASS |
| GET /api/clients → 401 | "GET /api/clients returns 401, not 404" | PASS |
| GET /api/client-update-requests → 401 | "GET /api/client-update-requests returns 401, not 404" | PASS |
| GET /api/comments → 401 | "GET /api/comments returns 401, not 404" | PASS |
| GET /api/deposits → 401 | "GET /api/deposits returns 401, not 404" | PASS |
| GET /api/handover-photos → 401 | "GET /api/handover-photos returns 401, not 404" | PASS |
| GET /api/handovers → 401 | "GET /api/handovers returns 401, not 404" | PASS |
| GET /api/lease-agreements → 401 | "GET /api/lease-agreements returns 401, not 404" | PASS |
| GET /api/meter-records → 401 | "GET /api/meter-records returns 401, not 404" | PASS |
| GET /api/notifications → 401 | "GET /api/notifications returns 401, not 404" | PASS |
| GET /api/notification-preferences → 401 | "GET /api/notification-preferences returns 401, not 404" | PASS |
| GET /api/pre-handover-inspections → 401 | "GET /api/pre-handover-inspections returns 401, not 404" | PASS |
| GET /api/sale-jobs → 401 | "GET /api/sale-jobs returns 401, not 404" | PASS |
| GET /api/task-statuses → 401 | "GET /api/task-statuses returns 401, not 404" | PASS |
| GET /api/tasks → 401 | "GET /api/tasks returns 401, not 404" | PASS |
| GET /api/tickets → 401 | "GET /api/tickets returns 401, not 404" | PASS |
| GET /api/warehouse-requirements → 401 | "GET /api/warehouse-requirements returns 401, not 404" | PASS |
| API health check | "API health endpoint confirms server is running" | PASS |
