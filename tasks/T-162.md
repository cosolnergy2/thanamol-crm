# T-162: Calibration detail + edit page

**Status:** In Progress
**Assignee:** dev
**Created:** 2026-04-01
**Updated:** 2026-04-01

### Objective

Create the detail and edit routes for calibration records under `calibrations/$calibrationId/`. The detail page must render all fields including the Calibration Results table. The edit form must pre-fill all existing data and allow modifying individual result rows.

Acceptance criteria:
- Detail page at `calibrations/$calibrationId/index.tsx` shows all header fields and a read-only results table
- Edit page at `calibrations/$calibrationId/edit.tsx` pre-fills all fields from the existing record
- Editing a result row (changing measured value) updates deviation and status immediately
- Save persists changes; verified by navigating back to detail page

### Detail

[Dev fills this — implementation plan, technical approach, files changed, doc URLs referenced]

### Definition of Done

- E2E test: navigate to calibration detail, verify all fields and results table display
- E2E test: edit calibration, modify a result row (change measured value), save, verify changes persist

### Test Results

[Tester fills this — pass/fail, screenshot paths, error logs if failed]
